#include<stdio.h>
void main()
{
    float a=22,b=7,sum,product,quotient;
    sum=a+b;
    product=a*b;
    quotient=a/b;
    printf("The sum is %f\nProduct is %f\nQuotient is %f",sum,product,quotient);
}